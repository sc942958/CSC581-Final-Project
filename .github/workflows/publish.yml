name: pipeline
on:
  push:
    branches:
      - "main"

jobs:
  build-push:
    name: build && push image to Docker Hub
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: setup docker buildx
        uses: docker/setup-buildx-action@v2

      - name: docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: build and push webserver
        id: build-push-webserver
        uses: docker/build-push-action@v3
        with:
          context: ./docker
          push: true
          tags: sethconley981/csc581-project-web-server:latest

  deploy:
    needs: build-push # Wait for the image to hit Docker Hub
    runs-on: self-hosted  # This tells GitHub to execute this on your CloudLab node!
    steps:
      - name: Deploy the updated stack
        run: |
          # The runner executes these commands directly on your CloudLab server
          docker compose pull web-server
          docker compose pull game-server
          docker compose up -d
